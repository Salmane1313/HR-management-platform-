services:
  db:
    image: postgres:16
    container_name: hr-platform-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: hr_platform
      POSTGRES_USER: hr_platform
      POSTGRES_PASSWORD: hr_platform
    ports:
      - "5432:5432"
    volumes:
      - hr-platform-pgdata:/var/lib/postgresql/data

volumes:
  hr-platform-pgdata: